// NfcScannerStyles.js
import { StyleSheet } from 'react-native';

// 스타일 정의
export const styles = StyleSheet.create({
  nfcSc: {
    backgroundColor: '#C0C0C0', // 버튼의 배경 색
    padding: 10, // 내부 여백
    marginTop: 10, // 버튼 상단의 마진
  },
});
